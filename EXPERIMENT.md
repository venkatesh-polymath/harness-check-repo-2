# EXPERIMENT BRIEF — round closeloop (full)

## THIS ROUND (do exactly this)
CLOSE-THE-LOOP EXPERIMENT — turn the leading indicator into a CAPABILITY. We showed effective rank leads plasticity collapse by ~4.75 tasks. Now USE that early warning: trigger an intervention when effective rank crosses the alarm threshold, and show it PREVENTS collapse better + more efficiently than a fixed-schedule reset. This is the payoff that converts an observation into a capability.

SETUP (fix prior confounds): 3-layer MLP (400-400, ReLU), SGD+momentum, BatchNorm OFF, Split-CIFAR-100 (10 tasks x 5 classes), real data at /opt/datasets (download=False, NEVER synthetic, ABORT if missing). IMPORTANT: use MORE steps/task than before — >=300 steps/task (reviewers flagged 50 steps as too sparse / dying-ReLU confound); verify dead-unit fraction at INIT is < ~15% (report dead_at_init). 8 seeds (10 if time). Same task splits across arms (paired).

FOUR ARMS (identical everything except the reset POLICY):
1. no_repair — vanilla, no reset (the collapsing floor).
2. fixed_reset — reset the lowest-utility units on a FIXED cadence (e.g. every task), standard CBP-style, utility = |out weight| x mean activation. Count total resets.
3. erank_triggered (OURS) — monitor effective rank of penultimate activations each task; when erank drops below an ALARM threshold (the early-warning level that precedes collapse), fire a targeted reset of the lowest-utility units. Resets fire ONLY when the alarm trips. Count total resets + WHEN they fired.
4. random_time (control) — fire the SAME NUMBER of resets as erank_triggered but at RANDOM tasks (to isolate whether the TIMING from the leading indicator matters, not just the count).

DELIVERABLES (the capability claims):
1. Does erank_triggered PREVENT/DELAY collapse? mean final-few-tasks accuracy per arm +/- 95% CI over seeds. Target: erank_triggered >> no_repair floor, and >= fixed_reset.
2. EFFICIENCY: does erank_triggered use FEWER resets than fixed_reset for equal-or-better accuracy? Report resets_fired per arm.
3. TIMING matters: does erank_triggered beat random_time (same reset count, wrong timing)? paired diff + CI + p. If yes, the leading indicator's TIMING carries the benefit — the core capability claim.

EXECUTION: ONE run_closeloop.py, single BLOCKING call `python run_closeloop.py 2>&1 | tee results/closeloop/run.log`, WAIT. 4 arms x 8 seeds x 10 tasks x >=300 steps, MLP = cheap; < 70 min. Write RESULTS.json incrementally + finalize.

OUTPUT results/closeloop/RESULTS.json status:"DONE":
{"status":"DONE","dataset":"cifar100_split","n_seeds":8,"steps_per_task":>=300,"dead_at_init":..,
 "arms":{"no_repair":{"mean_acc":..,"std":..,"resets":0},"fixed_reset":{"mean_acc":..,"std":..,"resets":..},"erank_triggered":{"mean_acc":..,"std":..,"resets":..,"reset_tasks":[..]},"random_time":{"mean_acc":..,"std":..,"resets":..}},
 "prevents_collapse": true/false, "triggered_vs_floor_pp":.., "triggered_vs_fixed_pp":{"mean":..,"ci95":[..]}, "triggered_vs_random_pp":{"mean":..,"ci95":[..],"p_one_sided":..,"timing_matters":true/false},
 "efficiency":{"triggered_resets":..,"fixed_resets":..,"triggered_uses_fewer":true/false},
 "notes":"does using effective-rank as an early-warning trigger PREVENT collapse better + more efficiently than fixed/random? honest."}
Do NOT leave status RUNNING. Commit all except datasets + weights.


Prior rounds' code and results are already committed under results/*/. Read them
for context and build on them; write this round's outputs under results/closeloop/.

## Idea
Observable Temporal Precedence Map: Which Cheap Signal Leads Plasticity Collapse — and by How Many Tasks?

## Prediction (registered before running)
Gradient-noise scale (McCandlish B_opt estimator, two mini-batches per step) will show the longest lead time (5–15 tasks before accuracy collapse) under SGD+momentum. Effective rank of penultimate-layer activations will be second (3–10 tasks). Dead-unit fraction will be third (1–5 tasks). Weight-norm drift will be near-contemporaneous with collapse (0–2 tasks). Under Adam, per-coordinate gradient normalization will suppress variance and collapse the noise-scale signal — making effective rank the dominant leading indicator. This optimizer-conditional reversal would explain why the existing rank/dead-unit-focused literature, which almost universally uses Adam, has not identified gradient-noise scale as predictive.

## Hypothesis chain (sanity gates FIRST)
  1. With a fixed random seed the entire training setup is deterministic: same initialization, same data ordering, same loss trajectory across two independent runs. → expect: Two runs launched with identical seed produce bit-identical per-step loss values and identical parameter checksums at run end. (pass if max(|loss_run1[t] - loss_run2[t]|) < 1e-6 for all logged steps; L2 distance between final parameter vectors = 0.0.)
  2. At random initialization, before any gradient update, cross-entropy loss on C classes equals log(C) (uniform-prediction prior), confirming no initialization leakage or accidental label memorization. → expect: Mean cross-entropy at step 0 is within ±0.05 of log(C) for CIFAR-10 (≈2.303) and CIFAR-100 (≈4.605) on a 1000-sample probe batch. (pass if abs(initial_loss - log(num_classes)) < 0.05 on the probe batch before any weight update.)
  3. A model trained on randomly permuted labels (input-independent baseline) cannot exceed chance accuracy, confirming that any accuracy gain in real training reflects genuine signal extraction rather than dataset artifacts. → expect: After full training on shuffled labels, test accuracy on shuffled-label data is within ±1 pp of 1/C (10% for CIFAR-10, 1% for CIFAR-100). (pass if Shuffled-label test accuracy in [1/C − 0.01, 1/C + 0.01] after 50 training epochs.)
  4. The optimizer and network can drive a single fixed batch to near-zero training loss when optimized repeatedly, confirming that gradient flow and architecture capacity are functional. → expect: Cross-entropy loss on a single fixed batch of 64 CIFAR-10 examples falls below 0.01 within 200 gradient steps using SGD+momentum (lr=0.01, m=0.9) on a 3-hidden-layer MLP (width 256, ReLU). (pass if loss_fixed_batch[step=200] < 0.01.)
  5. A single-task MLP trained from scratch on CIFAR-10 with SGD+momentum for 50 epochs reaches stable accuracy well above chance, establishing the non-sequential performance ceiling. → expect: Final test accuracy on CIFAR-10 exceeds 50%. (pass if test_accuracy >= 0.50 after 50 epochs; loss is still decreasing at epoch 10 (confirming optimization is not stuck).)
  6. Sequential training on a stream of 20 permuted-CIFAR-10 tasks with the same MLP and no reset produces measurable plasticity loss: later tasks achieve lower peak accuracy than earlier tasks. → expect: Mean peak accuracy on tasks 15–20 is at least 10 pp lower than peak accuracy on task 1. (pass if mean_peak_acc(tasks 15–20) < peak_acc(task 1) − 0.10, evaluated at each task's final training step.)
  7. Plasticity collapse can be pinned to a precise, pre-registered operational event t_collapse: the first task t at which new-task peak accuracy falls >15 pp below task-1 peak AND remains below that threshold for ≥2 consecutive tasks. → expect: t_collapse is identifiable (non-null) under this definition in every run on the 20-task stream, falling between tasks 5 and 18. (pass if t_collapse is non-null in all 5 seeds; t_collapse ∈ [5, 18] for ≥4/5 seeds; the 15-pp / 2-task criterion is hard-coded as a constant in the experiment config before any run.)
  8. Dead-unit fraction (fraction of ReLU units with zero activation on >95% of a 1000-sample probe set, measured on pre-normalization activations) is computable online and is monotonically non-decreasing across the 20-task stream under SGD. → expect: dead_frac[t+1] ≥ dead_frac[t] − 0.01 for all consecutive task pairs t in all 5 seeds; dead_frac[task_20] > dead_frac[task_1] by >0.10. (pass if No task-to-task decrease >0.01 across all seeds; final vs. initial gap >0.10 in ≥4/5 seeds.)
  9. Effective rank of penultimate-layer activations (exp(H) where H = entropy of the normalized singular-value distribution of the 1000-sample probe activation matrix, measured on pre-BN activations) decreases monotonically across the task stream under SGD. → expect: Effective rank at task 20 is at least 20% lower than at task 1 in ≥4/5 seeds. (pass if eff_rank[task_20] < 0.80 × eff_rank[task_1] in ≥4/5 seeds.)
  10. Gradient-noise scale B_opt (McCandlish 2018: trace(Σ)/tr(G), estimated using two independent mini-batches of 64 per step) is computable online under SGD+momentum with <30% wall-clock overhead and remains numerically stable throughout training. → expect: Wall-clock overhead vs. single-batch training is <30%; B_opt is finite, positive, and <1e8 at every logged checkpoint (every 50 steps) across all 5 seeds. (pass if overhead < 30%; 0 < B_opt < 1e8 at all logged steps in all 5 seeds; no NaN or Inf values.)
  11. Weight-norm drift (per-layer relative change ‖w_t‖/‖w_0‖ − 1, averaged across layers) is computable per task with zero extra forward passes and shows a non-trivial trend across the stream under SGD. → expect: Mean absolute weight-norm drift across layers at task 20 exceeds 10% in ≥4/5 seeds. (pass if mean_layer(|wt_norm_drift|)[task_20] > 0.10 in ≥4/5 seeds; computed from stored parameter norms with no added forward pass.)
  12. Observable onset is definable threshold-invariantly by changepoint detection on each z-scored trajectory: 'first task at which the signal deviates ≥k=3 robust-SDs from its early-training baseline (tasks 1–3) and remains deviated for ≥2 consecutive tasks.' This criterion is comparable across all four observables despite differing units. → expect: All four observables yield a non-null onset task under k=3 in ≥4/5 seeds; at least two distinct onset tasks appear across the four observables within each seed (they do not all fire simultaneously). (pass if onset_task non-null for all 4 observables in ≥4/5 seeds; ≥2 distinct onset values observed across the 4 observables in ≥4/5 seeds.)
  13. Under SGD+momentum, lead times (lead_time = t_collapse − onset_task) are positive for all four observables and follow the predicted temporal-precedence ordering: gradient-noise scale leads (5–15 tasks), then effective rank (3–10), then dead-unit fraction (1–5), then weight-norm drift (0–2). → expect: Median lead times fall within the predicted intervals; the strict ordering noise_scale > eff_rank > dead_frac > wt_norm holds as a chain in ≥4/5 seeds. (pass if median_lead(noise_scale) > median_lead(eff_rank) > median_lead(dead_frac) > median_lead(wt_norm); strict chain satisfied in ≥4/5 seeds; all lead times ≥0.)
  14. The temporal-precedence ordering under SGD is consistent across seeds: Kendall's W over the 4-observable ranking across 5 seeds exceeds 0.70, indicating near-agreement on which signal temporally precedes which. → expect: Kendall's W ≥ 0.70 with a bootstrapped 95% CI lower bound >0.50; if 0.50 ≤ W < 0.70 the result is explicitly framed as exploratory. (pass if W ≥ 0.70 → ordering confirmed; 0.50 ≤ W < 0.70 → reported as exploratory with uncertainty quantified; W < 0.50 → null hypothesis (random ordering) not rejected and experiment halted for redesign.)
  15. The precedence ordering is robust to the onset threshold k: swapping k from 3 to 2 or 4 does not change the top-ranked observable and alters at most one pairwise comparison in the SGD chain. → expect: Top-ranked observable (noise_scale) is identical at k=2, 3, 4 in ≥4/5 seeds; Kendall's W at k=2 and k=4 is within 0.15 of the k=3 value. (pass if noise_scale is top-ranked at all three k values in ≥4/5 seeds; |W(k=2) − W(k=3)| < 0.15 and |W(k=4) − W(k=3)| < 0.15.)
  16. Under Adam (lr=1e-3, β₁=0.9, β₂=0.999), per-coordinate gradient normalization suppresses variance in the B_opt estimator, causing gradient-noise scale to show no onset under the k=3 criterion (null onset) in the majority of seeds. → expect: noise_scale onset_task is null in ≥3/5 seeds under Adam; dead-unit fraction and effective rank onsets remain non-null in ≥4/5 seeds. (pass if noise_scale onset null in ≥3/5 seeds under Adam; dead_frac and eff_rank onset non-null in ≥4/5 seeds under Adam.)
  17. Under Adam, effective rank of penultimate-layer activations becomes the longest-lead temporal indicator in place of gradient-noise scale, consistent with the optimizer-conditional reversal hypothesis. → expect: median_lead(eff_rank) is the maximum non-null lead time in ≥3/5 seeds under Adam; eff_rank lead time under Adam exceeds its lead time under SGD by ≥2 tasks (median). (pass if eff_rank is the top-ranked non-null observable in ≥3/5 seeds under Adam; median_lead_Adam(eff_rank) ≥ median_lead_SGD(eff_rank) + 2.)
  18. Under BatchNorm, post-BN activations artificially suppress dead-unit counts (BN re-centers outputs), so pre-BN activations must be used for dead-unit fraction and effective rank to remain apples-to-apples with the no-BN condition. → expect: post_BN_dead_frac stays <0.05 throughout all 20 tasks (uninformative); pre_BN_dead_frac[task_20] > pre_BN_dead_frac[task_1] + 0.10, matching the no-BN trend. (pass if post_BN_dead_frac[task_20] < 0.05 in ≥4/5 seeds with BN; pre_BN_dead_frac gap >0.10 in ≥4/5 seeds; both confirmed before any BN-vs-noBN comparison is drawn.)
  19. Adding LayerNorm vs. no normalization does not change the within-optimizer precedence ordering: the SGD leader (noise_scale) and Adam leader (eff_rank) are stable across LN-on and LN-off conditions, making optimizer the primary axis of config-conditionality. → expect: Within SGD runs (LN and no-norm): noise_scale is top-ranked in ≥4/5 seeds in both. Within Adam runs (LN and no-norm): eff_rank is top-ranked in ≥4/5 seeds in both. Kendall's W ≥ 0.60 in all four groups. (pass if Top-ranked observable matches the optimizer-expected leader in ≥4/5 seeds in all four (optimizer × norm) groups; W ≥ 0.60 in each group.)
  20. The optimizer-conditional leading-indicator pattern (SGD→noise_scale leads; Adam→eff_rank leads) generalizes beyond permuted-CIFAR-10: the same optimizer-conditional top-ranked observable is recovered on a sequential-SVHN task stream. → expect: On SVHN under SGD: noise_scale is top-ranked in ≥4/5 seeds. On SVHN under Adam: eff_rank is top-ranked in ≥4/5 seeds. (pass if Both optimizer-conditional top-ranked observables match the CIFAR-10 result on SVHN in ≥4/5 seeds per optimizer; if either fails, scope is narrowed to CIFAR-10 only and documented as a boundary condition.)
Sanity gates: fixed seed, verify loss at init, input-independent baseline, overfit one batch to ~0 loss

## Study spec
- goal: Quantify the lead time in tasks by which each of four cheap signals (dead-unit fraction, activation effective rank, gradient-noise scale, weight-norm drift) precedes plasticity collapse on Split-CIFAR-10, and test whether that temporal-precedence ordering is consistent or config-conditional across a 2×2 optimizer × BatchNorm factorial.
- scientific (measure): ['optimizer ∈ {Adam, SGD} — tests the falsifiable sub-hypothesis that Adam suppresses the gradient-noise-scale lead signal relative to SGD, potentially inverting its rank in the precedence ordering', 'batchnorm ∈ {on, off} — tests whether BN confounds dead-unit fraction and effective rank enough to alter the precedence ordering; all four observables measured on pre-BN (pre-normalization) activations in both arms so the measurement node is identical across the BN factor']
- nuisance (tune per arm): ['learning_rate: tuned independently per optimizer arm to match early-task (tasks 1–3) loss-AUC within ±5% of the baseline arm; Adam grid [1e-4, 3e-4, 1e-3], SGD grid [1e-2, 3e-2, 1e-1]; selected value held fixed across all tasks in that arm', "weight_decay: tuned per optimizer arm to match the baseline arm's early-task weight-norm trajectory (Adam: [0, 1e-4, 1e-3]; SGD: [0, 1e-3, 5e-3]); ensures weight-norm drift observable is not artificially inflated or suppressed by regularization asymmetry", 'gradient_noise_scale estimator mini-batches: tuned per BN arm to achieve <5% relative SE on the GNS estimator (BN alters per-sample gradient variance); target sample size confirmed on task 1 before main runs']
- fixed (hold constant): ['architecture: 3-layer MLP (400–400 hidden units, ReLU) matching Dohare et al. CBP paper exactly — caveat: precedence ordering may not transfer to convolutional or attention-based architectures', 'dataset and task sequence: Split-CIFAR-10, 10 tasks × 2 disjoint classes, class permutation fixed at seed=0 across all arms', 'steps per task: 2 000 gradient steps, matching CBP paper protocol', 'random seeds: 5 seeds {0,1,2,3,4} — caveat: thin for strong consistency claims; first pass is explicitly exploratory; Kendall W reported with 95% bootstrap CIs', 'collapse event (t_collapse): first task t at which new-task test accuracy is ≥20 pp below task-1 accuracy AND remains below for ≥2 consecutive tasks; uses trainability (new-task) accuracy per reviewer requirement; fixed before any run', "onset criterion: PELT changepoint on each z-scored per-signal trajectory, z-scored against the tasks 1–5 baseline window; onset = first task the signal deviates ≥k robust-SDs from baseline and stays deviated; primary k=2.0, mandatory sensitivity sweep k∈{1.5, 2.5} — any precedence-rank flip across k values flagged as threshold-sensitive and excluded from the 'consistent ordering' claim", 'lead-time metric: lead_time(signal) = t_collapse − onset(signal) in integer tasks; positive = signal fires before collapse; framing is predictive temporal precedence, NOT causal', 'sanity-check protocol (all four must pass before main runs proceed): (1) two independent fixed-seed runs produce bit-identical loss trajectories; (2) CE loss at random init = log(10) ± 0.01; (3) permuted-label arm accuracy ≤ 12% throughout all 10 tasks; (4) 500 steps on one fixed batch drives loss to <0.01', 'compute budget: ~60 A10G-hours total per original estimate; per-run wall-clock logged and reported']
- baseline: Vanilla continual learning: 3-layer MLP (400–400, ReLU), SGD with nuisance-tuned LR and weight decay, BatchNorm OFF, Split-CIFAR-10, 10 tasks, 5 seeds, zero plasticity repair. Replicates the collapse trajectory documented in Dohare et al.; establishes the reference t_collapse distribution and all four signal trajectories before any optimizer or BN factor is introduced. Built and verified first.
- eval contract: Dataset: Split-CIFAR-10 (10 tasks × 2 classes, fixed permutation seed=0). Primary metric: lead_time(signal) = t_collapse − onset(signal) in tasks, per signal × optimizer × BN arm, reported as mean ± 95% bootstrap CI over 5 seeds. Precedence-ordering consistency: Kendall's W (coefficient of concordance) over the 5 per-seed signal orderings within each optimizer × BN cell; W ≥ 0.70 → 'consistent ordering' (pre-registered threshold); W < 0.70 → 'config-conditional or noisy.' Pairwise lead-time differences between the 4 signals tested with Wilcoxon signed-rank (paired over seeds, Bonferroni-corrected for 6 pairs, α=0.05). Sensitivity: full ordering re-derived at k∈{1.5, 2.5} alongside k=2.0; any rank flip reported as a finding, not suppressed.

Record everything under results/closeloop/. Do not commit weights.
